export enum TreeSelectionMode {
  SINGLE = "single",
  MULTIPLE = "multiple",
}

export enum TreeDragDropMode {
  NONE = "none",
  VERTICAL = "vertical",
  HORIZONTAL = "horizontal",
  BOTH = "both",
}

export enum DragDirection {
  VERTICAL,
  HORIZONTAL,
}
